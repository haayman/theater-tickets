# Logging Configuration
# Enable HTTP request logging (Morgan -> Winston)
# Set to "true" to enable HTTP logging (default: disabled)
ENABLE_HTTP_LOGGING=true

# Log level for Winston (error, warn, info, debug, verbose)
LOG_LEVEL=info
